/*
 * Single source of truth for the displayed extension version. The footer in
 * the editor UI renders this value; a test asserts it stays in sync with
 * package.json and manifest.json.
 */
export const VERSION = "2.0.0";
